@model User
@{
    ViewBag.Title = Dictionary.SupportUs;
    ViewBag.SubTitle = Dictionary.SupportUs;
}

<div id="main-banner" class="banner-default main-menu">
    @Html.Partial("_Navbar")
</div>
@Html.Partial("_SupportUs")