@model K9.Base.WebApplication.ViewModels.ArchiveViewModel
@{
    ViewBag.Title = Dictionary.Archive;
}

<div id="main-banner" class="banner-forest3 main-menu">
    @Html.Partial("_Navbar")
</div>
<div class="main-section">
    <div class="container">
        <div class="main-heading">
            <h3>@Dictionary.Archive</h3>
        </div>
    </div>
    <div class="container">
        <div>
            <div class="col-md-3">
                @Html.Partial("_ArchiveMenu")
            </div>
            <div class="col-md-9">
                <div class="archive-content-container">
                    @if (Model.SelectedArchive != null)
                    {
                        foreach (var item in Model.SelectedArchive.Items)
                        {
                            @Html.Partial("_ArchiveItem", item)
                        }
                    }
                </div>
            </div>
            <div class="clearfix">
            </div>
        </div>
    </div>
</div>
